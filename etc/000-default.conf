<VirtualHost *:80>
# The ServerName directive sets the request scheme, hostname and port that
# the server uses to identify itself. This is used when creating
# redirection URLs. In the context of virtual hosts, the ServerName
# specifies what hostname must appear in the request's Host: header to
# match this virtual host. For the default virtual host (this file) this
# value is not decisive as it is used as a last resort host regardless.
# However, you must set it for any further virtual host explicitly.
#ServerName www.example.com

ServerAdmin webmaster@localhost
DocumentRoot /var/www/html

# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
# error, crit, alert, emerg.
# It is also possible to configure the loglevel for particular
# modules, e.g.
#LogLevel info ssl:warn

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

# For most configuration files from conf-available/, which are
# enabled or disabled at a global level, it is possible to
# include a line for only one particular virtual host. For example the
# following line enables the CGI configuration for this host only
# after it has been globally disabled with "a2disconf".
#Include conf-available/serve-cgi-bin.conf

# DirectoryIndex index.php index.html index.htm    //访问网站时默认打开的文件
# ErrorLog "logs/www.aliyun.com-error_log"       //日志
# CustomLog "logs/www.aliyun.com-access_log" common //日志
</VirtualHost>

# 自定义开始

<VirtualHost *:80>
ServerName lav.aaa
DocumentRoot /home/wuming/www/laravel55/public
</VirtualHost>


<VirtualHost *:80>
ServerName yii.aaa
DocumentRoot /home/wuming/www/yiidev/web
<Directory "/home/wuming/www/yiidev/web">
# 开启 mod_rewrite 用于美化 URL 功能的支持（译注：对应 pretty URL 选项）
RewriteEngine on
# 如果请求的是真实存在的文件或目录，直接访问
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# 如果请求的不是真实文件或目录，分发请求至 index.php
RewriteRule . index.php
</Directory>
</VirtualHost>

<VirtualHost *:80>
     DocumentRoot /home/wuming/www/WordPress
     ServerName wp.aaa

     <Directory /home/wuming/www/WordPress>
        Options +FollowSymlinks
        AllowOverride All
        Require all granted
     </Directory>
</VirtualHost>

<VirtualHost *:80>
ServerName test.aaa
DocumentRoot /home/wuming/www/test

# RewriteEngine on
# AllowOverride All
# Allow from all
</VirtualHost>

# <VirtualHost *:80>
# ServerName wordpress.aaa
# DocumentRoot /home/wuming/www/WordPress

# Options Indexes FollowSymLinks
# AllowOverride All
# Order Allow,Deny
# Allow from all
# RewriteEngine on
# # RewriteEngine on
# # AllowOverride All
# # Allow from all

# </VirtualHost>


# <VirtualHost *:80>
#     DocumentRoot "/home/wuming/www/WordPress"
#     ServerName wordpress.aaa
#     # ServerAlias www.pptv.cn #可省略
#     <Directory "/home/wuming/www/WordPress">
#         Options Indexes FollowSymLinks
#         AllowOverride All
#         Order Allow,Deny
#         Allow from all
#         RewriteEngine on
#         RewriteCond %{REQUEST_FILENAME} !-d
#         RewriteCond %{REQUEST_FILENAME} !-f
#         RewriteRule ^(.*)$ index.php/$1 [QSA,PT,L]
#     </Directory>
# </VirtualHost>

<VirtualHost *:80>
ServerName h5.aaa
DocumentRoot /home/wuming/www/h5
</VirtualHost>

<VirtualHost *:80>
ServerName php.aaa
DocumentRoot /home/wuming/www/phptutorial
</VirtualHost>

